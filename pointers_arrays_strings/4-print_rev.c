#include "main.h"
/**
 *function that prints a string, in reverse, followed by a new line.
 *Return: 0
 *@s: Pointer
 */
void print_rev(char *s);
{
	int i
	for(i = '\0'; str[i] != '\0'; i--)
	{
	_putchar(str[i]);
	}
	_putchar('\n');
}
